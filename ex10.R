# CONTROL FLOW
    # IF
    # FOR .. LOOP
    # WHILE .. LOOP

# WHILE .. LOOP

x <- 1
# important, initialize starting point

while (x <= 10) {
    print(x)
    x <- x+1
}

# add string
a = "Namchok"
b = "Singhachai"
paste(a, b)

x <- 1
while (x <= 10) {
    print(paste(x, "less than or equal to ten"))
    x <- x+1
}